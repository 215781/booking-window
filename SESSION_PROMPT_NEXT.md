# Session Prompt — whentobook.co.uk fixes + PLAN_V2 completion

---

## ⚠️ BEFORE ANYTHING ELSE — START-OF-SESSION VERIFICATION

You are **Orchestrator**. Before doing any work, run this:

```bash
cd /Users/connormartin/booking-window
git merge-base --is-ancestor 322bf0c HEAD && echo "OK" || echo "MISMATCH — investigate before starting"
git log main -1 --oneline
```

Expected: `OK` and HEAD should be `9d35ab3` or a later commit that descends from it. If MISMATCH, stop and diagnose before proceeding.

Then read `NEXT_SESSION_PROMPT.md` and `PLAN.md` in full.

---

## ⚠️ PUSH PROTOCOL — THIS SESSION DOES NOT END WITHOUT THIS

**At the end of this session, the Scribe must:**
1. Commit all docs (`PLAN.md`, `NEXT_SESSION_PROMPT.md`)
2. Run `git push origin main` — must return exit code 0
3. Run `git log origin/main -1 --oneline` and record the hash in `NEXT_SESSION_PROMPT.md`
4. Report back: "Push confirmed. HEAD: [hash]"

The session is not complete until that confirmation is received. No exceptions. See `SCRIBE.md` for the full protocol.

---

## Context — what this project is

**whentobook.co.uk** — Club Med ski resort price intelligence site. GitHub Pages. Single-file HTML/CSS/JS at `clubmed/index.html` (no build tools, no frameworks). Price data in `_data/prices_clubmed.csv` (append-only, ~9,000+ rows). Builder runs at `/Users/connormartin/booking-window/` (main repo — never a worktree). SSH key: `~/.ssh/booking_window_deploy`.

### Architecture warning — read before touching `clubmed/index.html`

`clubmed/index.html` is both hand-edited AND can be regenerated by `python clubmed_checker.py --inject-only`. **Never run `--inject-only` from a worktree** — it will regenerate from a stale template and wipe all recent changes. If you need to regenerate RESORT_DATA, run `--inject-only` from the main repo only, after `git pull`.

**Before any push touching `clubmed/index.html`, verify:**
```bash
grep -c "RESORT_IMAGES" clubmed/index.html    # must be > 0
grep -c "renderHeroBestCard" clubmed/index.html  # must be > 0
```
If either is 0, the file has regressed. Do not push — investigate.

### Why features keep disappearing

A 15-day-old commit was cherry-picked onto main, regenerating `clubmed/index.html` from a stale template. This destroyed resort images, hero best-card, and signal-first CSS. Full forensic report in `GIT_DIAGNOSIS.md`. The rules above prevent a repeat.

---

## What needs fixing — 12 tasks

Work through these in order. Each one is a separate Builder task with its own commit.

---

### TASK 1 — Diagnose and fix blank hero best-card and empty resort cards

**Problem:** The "best price resort" card at the top of the page is blank. All resort cards show no data. Before the May 19 session there was rich price data (price changes, departure dates, currency amounts).

**Root cause to investigate:** `RESORT_DATA` in `clubmed/index.html` (around line 1762) may have been injected with empty/placeholder entries by a stale `--inject-only` run. Alternatively, `getBestOpportunity()` may return null because no resort has a 14-day delta.

**What to do:**
1. Check the actual contents of the `RESORT_DATA` array in `clubmed/index.html` — do the resorts have `departures` arrays with real price values, or are they empty/placeholder?
2. Check `_data/prices_clubmed.csv` — does it have recent rows for the ski resorts (TIGC_WINTER, ARPC_WINTER, etc.)?
3. If RESORT_DATA is empty/stale: run `python clubmed_checker.py --inject-only` **from the main repo** (`/Users/connormartin/booking-window/`) to rebuild it from the CSV
4. If `getBestOpportunity()` returns null because all deltas are 0: check the function logic (around line 4966) — it should fall back to showing the resort with the lowest current price, not go blank
5. Fix `renderHeroBestCard()` to never render blank — if no signal data exists yet, show the resort with the lowest current price and a note that signal data is building

**Acceptance criteria:** Hero best-card shows a real resort with a real price. All 11 ski resort cards show their departure dates and prices (even if the price-change delta is 0).

---

### TASK 2 — Fix form opt-in checkbox

**Problem:** The consent checkbox in the booking alert form does not work (likely cannot be checked, or checking it does nothing).

**Location:** Around line 694/709 in `clubmed/index.html` — `.consent-label input[type="checkbox"]`. Also check lines 1621, 1689, 5731 (there are three checkbox instances: bottom-of-page form, search results popup, mobile FAB).

**This is PLAN_V2 task B15.** The full brief is in `PLAN_V2.md` under B15.

**Likely cause:** CSS may be hiding the native checkbox and a custom replacement isn't receiving click events. Or the `required` validation is checking the wrong element ID.

**Acceptance criteria:** User can tick all three consent checkboxes. Form submit is blocked if unchecked. No visual glitch on check/uncheck.

---

### TASK 3 — Replace page-embedded form with FAB (floating action button)

**Problem:** The user wants the email signup form removed from the main page body. Instead: a floating button (like a live chat bubble, always visible in the corner) that opens the form in a slide-up panel or small modal.

**Context:** A mobile FAB was already built in commit `30fad1a` (visible on mobile). The user wants this extended to all screen sizes and wants the in-page form sections removed.

**What to do:**
1. Remove the inline email signup form section from the main page body (`clubmed/index.html`) — the "Get a booking alert" section that currently appears between the resort cards and the footer
2. Keep the existing mobile FAB (floating button + slide-up panel from commit `30fad1a`) and extend it to be visible on desktop too (not just mobile)
3. The FAB button: fixed position, bottom-right corner, teal background (`#1a4a42`), letter/bell icon, visible on all screen sizes
4. Clicking the FAB opens the slide-up/modal form — use the Kit form (`7f784a323c` — `https://app.kit.com/forms/7f784a323c/subscriptions`)
5. The form must include the working consent checkbox from Task 2
6. The search results popup form (`f197f8f414`) is separate — keep that in place, do not remove it

**Acceptance criteria:** No inline form in page body. FAB visible on mobile and desktop. Clicking FAB opens form. Form submits to Kit. Checkbox works.

---

### TASK 4 — Unified header: dark teal, Blog + About Us in nav

**This is PLAN_V2 task B1.**

**Problem:** The tracker page (`clubmed/index.html`) uses a light sticky nav. The blog posts and blog index use a dark teal `<header>`. The user wants one consistent header everywhere.

**Decision (A4 in PLAN_V2):** ✅ Use the dark teal header from the blog across the tracker too.

**What to do:**
1. Look at `_layouts/post.html` or `blog/index.html` for the current dark teal header HTML/CSS
2. Apply that same header to `clubmed/index.html` — replace the current light nav with the dark teal version
3. The nav must include links: **Home** (`/`), **Club Med** (`/clubmed`), **Blog** (`/blog`), **About** (`/about`)
4. The "When To Book" logotype styling must match across all pages
5. Ensure the header does not break the hero section's layout on mobile

**Acceptance criteria:** `clubmed/index.html` has the dark teal header. Nav includes Blog and About Us. Visually consistent with the blog pages.

---

### TASK 5 — Remove party size filter tabs

**Problem:** The "2 Adults / +1 Child / +2 Children" filter buttons above the resort cards should be removed. The site tracks 2A pricing — just show it without filtering UI.

**Location:** Lines 1518–1520 in `clubmed/index.html`:
```html
<button class="filter-btn active" data-party="2A">2 Adults</button>
<button class="filter-btn" data-party="2A1C">+1 Child</button>
<button class="filter-btn" data-party="2A2C">+2 Children</button>
```

**What to do:**
1. Remove the three filter buttons and their wrapping `<div>` from the HTML
2. Remove the JS event listener that updates `activePartySize` (around line 5363–5368)
3. Hard-code `activePartySize = '2A'` as a constant — the page should always render 2-adult pricing
4. Remove any CSS for `.party-filter` or `.filter-btn[data-party]` that becomes dead code

**Acceptance criteria:** No party size tabs visible. Cards render 2-adult pricing only. No JS errors.

---

### TASK 6 — Remove sort bar, always sort by biggest price drop

**Problem:** The "Lowest price first / Highest price first / Biggest price drop" sort bar should be removed. Resorts should always be ordered by biggest price drop first.

**Location:** Lines 1523+ in `clubmed/index.html` — the `.sort-bar-wrap` div with three `.filter-btn` buttons.

**What to do:**
1. Remove the sort bar HTML (`<div class="sort-bar-wrap">` and its contents)
2. In the `renderCards()` function, hard-code the sort to always sort by 14-day price drop (largest drop first, i.e. most negative `delta14` first)
3. Remove the `activeSortMode` variable and any JS that updates it
4. Remove the sort bar CSS (`.sort-bar-wrap`, related styles) that becomes dead code

**Acceptance criteria:** No sort bar visible. Cards always appear in biggest-price-drop order. No JS errors.

---

### TASK 7 — Remove "Stable over 14 days" text from resort cards

**Problem:** When a resort's price hasn't moved, the card shows "— Stable over 14 days". The user wants this removed — just show nothing when stable.

**Location:** Line 5058 in `clubmed/index.html`:
```javascript
movementHTML = `<span class="price-movement price-stable">— Stable over 14 days</span>`;
```
Also line 5530: `'Stable over 14 days'`

**What to do:**
1. Replace the stable case with an empty string: `movementHTML = '';`
2. Check all locations where "Stable over 14 days" appears and remove them
3. Do NOT remove the `.price-movement` CSS — it's still used for up/down cases

**Acceptance criteria:** When a resort has no 14-day price movement, nothing is shown in the movement slot. The up (↑) and down (↓) cases continue to work.

---

### TASK 8 — Price change = dominant figure on resort cards

**Problem:** On the resort cards, the price change amount (e.g. "↓ £240 in 14 days") should be the largest, most prominent figure the user sees first. The current price itself (e.g. "£3,240") should be secondary/smaller.

**Current state:** `.price-movement` is 22px Playfair Display 700 (restored this morning). `.card-price` is 14px Inter muted. This is the correct hierarchy — confirm it's actually rendering that way.

**What to do:**
1. Verify the CSS is correct: `.price-movement` should be visually dominant (22px+ Playfair Display bold, full colour) and `.card-price` should be clearly secondary (14px Inter, muted colour)
2. Check the card layout — the price-movement should appear ABOVE or BEFORE the card-price in the visual hierarchy
3. If the layout order in the HTML puts card-price before price-movement, reorder them
4. Make sure this looks correct on both mobile and desktop

**Acceptance criteria:** On a resort card with a price drop, "↓ £240 in 14 days" is visually the dominant element. The current price "£3,240" reads as supporting information below it.

---

### TASK 9 — Fix resort cards not opening on click

**Problem:** Clicking a resort card does not open the price history modal.

**Location:** Lines 5094–5104 in `clubmed/index.html` — the `click` and `touchend` event listeners on resort cards.

**What to investigate:**
1. Is the click listener being attached after the cards render? Check whether `renderCards()` is adding listeners after the DOM is ready
2. Is there a CSS element covering the card and intercepting clicks? (e.g. an overlay, pseudo-element, or the FAB button's area)
3. Is `openModal()` (line 5289) being called but failing silently?
4. Check browser console for JS errors when clicking a card

**Acceptance criteria:** Clicking any resort card opens the modal with price history for that resort. Works on both mobile (touch) and desktop (mouse click).

---

### TASK 10 — Restore summer resorts with ski/summer toggle

**Problem:** The site previously had a toggle to switch between ski resorts and summer resorts. This has been removed and summer resorts are not displayed.

**Summer resort codes** (all have data in `_data/prices_clubmed.csv`):
`CARC`, `MMAC`, `LPAC`, `AGAC`, `TURC`, `KANC`, `GREC`, `PALC`, `LAPC`, `DBAC`

**What to do:**
1. Check `prices_clubmed.csv` — confirm summer resort rows exist (they do — committed as "data: CARC summer prices 2026-05-19" etc.)
2. Check `clubmed_checker.py` — confirm summer resorts are in the checker's resort list and their names/codes are correct
3. Run `python clubmed_checker.py --inject-only` from `/Users/connormartin/booking-window/` to rebuild RESORT_DATA including summer resorts
4. Add a ski/summer toggle tab to the tracker page — two tabs above the resort cards: "⛷ Ski" and "☀ Summer". Clicking a tab filters the cards to show only ski or summer resorts
5. Default tab: Ski (winter resorts). Summer tab shows the summer resort cards.
6. The hero best-card should respond to the active tab (show best ski opportunity when on ski tab, best summer opportunity on summer tab)

**Architecture note:** The summer resorts have their own departure days and price structures. Do NOT run `--inject-only` from a worktree — run it only from the main repo after pulling latest.

**Acceptance criteria:** Two tabs visible. Ski tab shows 11 ski resorts. Summer tab shows the summer resorts with their price data. Toggle switches correctly. Hero card updates.

---

### TASK 11 — Complete all outstanding PLAN_V2 agent tasks

Read `PLAN_V2.md` in full. Identify which Section B tasks have NOT yet been completed. Prioritise them according to the Phase ordering in PLAN_V2. Complete each outstanding task in a separate Builder commit.

**Known status (verify by reading PLAN_V2.md):**
- B1 (unified header) — ✅ done in this session (Task 4 above)
- B2 (reorder tracker sections) — status unknown, check
- B3 (About page) — built, verify it's live and correct
- B4 (affiliate disclosure page) — built, verify
- B5 (expand footer) — status unknown, check
- B6 (founding story pull quote in hero) — status unknown, check
- B7 (rewrite hero subtitle + signal descriptions) — status unknown, check
- B8 (fix JS modal narrative language) — status unknown, check
- B9 (fix article closing lines) — built, verify
- B10 (Val d'Isère "22 departure dates" claim) — status unknown, check
- B11 (articles 11-13) — built, verify
- B12 (mobile audit: hero best-card) — status unknown, check
- B13 (fix Summer nav link) — status unknown, check (summer tab in Task 10 may satisfy this)
- B15 (Kit form + checkbox) — done in Task 2 above

For each incomplete task: brief the Builder, get it done, have Scribe mark it complete.

---

## Design rules (locked — do not change)

- Background `#f5f0e8` · Primary teal `#1a4a42` · Secondary amber `#8a6a2a`
- Fonts: Playfair Display (display/headings) + Inter (body)
- Signal badges: Favourable = teal, Watch = amber, Hold = grey `#bbb5aa`
- `DATA_SUFFICIENT = false` — badges show "Building data — check back in autumn" — **do not change**
- Language: never use "deals", "discounts", "cheap", "vouchers", "savings"
- Always use: "booking intelligence", "optimal timing", "historically favourable pricing"

---

## End of session checklist

Before calling the session done, confirm with the Scribe:

- [ ] `git log origin/main -1 --oneline` shows the expected final commit
- [ ] `grep -c "RESORT_IMAGES" clubmed/index.html` > 0
- [ ] `grep -c "renderHeroBestCard" clubmed/index.html` > 0
- [ ] `NEXT_SESSION_PROMPT.md` updated with new HEAD hash and full list of commits
- [ ] Push confirmed, hash recorded
